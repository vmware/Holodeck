name: Links

on:
  push:
    branches:
      - main
      - 'release/**'
  pull_request:
    branches:
      - main
      - 'release/**'
  repository_dispatch:
  workflow_dispatch:
  schedule:
    - cron: "00 18 * * 1"

jobs:
  linkChecker:
    runs-on: ubuntu-latest
    permissions:
      issues: write
    steps:
      - uses: actions/checkout@v7

      - name: Restore lychee cache
        id: restore-cache
        uses: actions/cache/restore@v6
        with:
          path: .lycheecache
          key: cache-lychee-${{ github.sha }}
          restore-keys: cache-lychee-

      - name: Link Checker
        id: lychee
        uses: lycheeverse/lychee-action@v2
        with:
          args: --cache --max-cache-age 7d --verbose --no-progress './**/*.md'
          fail: false

      - name: Save lychee cache
        uses: actions/cache/save@v6
        if: always()
        with:
          path: .lycheecache
          key: ${{ steps.restore-cache.outputs.cache-primary-key }}

      - name: Create or Update Issue
        if: steps.lychee.outputs.exit_code != 0
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          ISSUE_TITLE="Link Checker Report - ${{ github.ref_name }}"
          ISSUE_NUMBER=$(gh issue list --state open --label "automated issue" --search "$ISSUE_TITLE" --json number,title --jq --arg title "$ISSUE_TITLE" '.[] | select(.title == $title) | .number' | head -1)
          if [ -n "$ISSUE_NUMBER" ]; then
            gh issue edit "$ISSUE_NUMBER" --body-file ./lychee/out.md
          else
            gh issue create --title "$ISSUE_TITLE" --body-file ./lychee/out.md --label "report" --label "automated issue"
          fi
